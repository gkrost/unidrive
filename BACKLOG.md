# Backlog

Top of file = next up. Work down. Move done items to `CLOSED.md` in the same commit. No IDs, no dates, no versions.

## Critical — data-risk

Silent corruption, orphan storage, lost local metadata. Fix before anything else.

| Title | Scope |
|---|---|
| Tracking-set sync engine | Replace `state.db`-as-authoritative with a tracking-set predicate over deletion. Untracked paths are invisible to deletion logic; the `.safe/` phantom-row delete-cascade class of bug becomes structurally impossible by construction. Lands at `core/app/sync-tracking/` as a new module; legacy `core/app/sync/` enters a retirement path once the new engine has provider-integration parity. Lemma + regression test for the `.safe/` incident shape are non-negotiable parts of the smoke set. Identity moves to `(provider_id, remote_file_id)` with content-hash as rename heuristic; first-run non-empty `sync_root` uses adopt-on-exact-content-match with LOUD collisions; batch-level max-delete-ratio guard backstops the residual "provider transiently lied" case. Spike + prototype exist as branches in the legacy repo (`spike/tracking-set-reconcile`, `prototype/tracking-set-engine`) — relocate, don't rewrite from scratch. |

## High — correctness, required for first release

| Title | Scope |
|---|---|
| Daemon constructs `SyncEngine` with `syncRoot = dbPath.parent` — wrong, `refresh.run` fails immediately on every real profile | Surfaced during 2026-05-25 live smoke. `DaemonRuntime.start()` at `core/app/cli/src/main/kotlin/org/krost/unidrive/cli/DaemonRuntime.kt:112` does `SyncEngine(provider, db!!, syncRoot = dbPath.parent)`. `dbPath.parent` is the provider-config directory (`~/.config/unidrive/<profile>`), NOT the actual sync_root (`~/Onedrive` or wherever `config.toml [providers.<profile>].sync_root` points). On the first `refresh.run`, `SyncEngine.doSyncOnce` at `SyncEngine.kt:447` notices the mismatch with the persisted state.db and aborts with `IllegalStateException: sync_root changed from '<old>' to '<new>'. Run with --reset to wipe state and re-sync from scratch.` The exception propagates as `provider_error` to the refresh client. Plan-prescribed code from Task 5 + Task 8; neither task's tests caught it because the test fixture uses a tempdir for both db and sync_root (`dbPath.parent == tempDir` happens to equal the stub's syncRoot). Live smoke against `posteo_onedrive` was the first time it fired. **Already being fixed in this branch** during continued live smoke — F7 fix thread the real `syncRoot` from `parent.loadSyncConfig().syncRoot` through `DaemonRuntime` constructor + `DaemonRunCommand`; tests pass tempdir as before. Reference: SyncCommand at SyncCommand.kt:232 already does `config = parent.loadSyncConfig()` then `SyncEngine(syncRoot = config.syncRoot, ...)` at line 404-407. Priority **High** — `refresh.run` fully non-functional on real profiles until fixed. |
| Mount doesn't survive daemon restart — operations return EIO until mount is manually remounted | Surfaced during 2026-05-25 live smoke. Workflow: daemon up → mount up → `unidrive daemon stop` (graceful) → `unidrive daemon run` (fresh daemon, new PID, same socket path) → `ls /tmp/onedrive-smoke` returns `Input/output error (os error 5)`. The mount itself stays mounted (kernel still sees the FUSE mountpoint), but the Rust co-daemon's `ReconnectingIpcClient` does NOT auto-reconnect after the daemon-restart cycle. Spec §5.3 ("daemon restart while mount is running is handled by that pre-existing client-side machinery") was optimistic — the existing `ReconnectingIpcClient` handles socket-restart on the NEXT op only if the connection is still considered "live" by the Rust side; after the disconnect, subsequent ops surface EIO. Additionally, the new daemon's `StaleMountDetector` flags the still-active mount as `WARNING: detected 1 stale unidrive FUSE mount(s)`, which is misleading — the mount is not stale, it's the connected client of the previous daemon, currently in EIO-stuck state. Two scope items: (a) Rust co-daemon: extend `ReconnectingIpcClient` to retry on `Disconnected` even when the previous op hit ECONNREFUSED; bound the retry to a deadline (~30s) before surfacing EIO to the kernel. (b) JVM `StaleMountDetector` (`core/app/cli/src/main/kotlin/org/krost/unidrive/cli/StaleMountDetector.kt`): refine "stale" definition to exclude mounts whose backing Rust co-daemon PID is still alive (parse `/proc/self/mounts` for the device PID, or check process tree for active `unidrive-mount` binary serving that mountpoint). Acceptance: daemon stop+start cycle preserves mount functionality without remount; StaleMountDetector only fires on TRULY stale mounts (no co-daemon process alive). Priority **High** — operator-visible reliability gap for the daemon-restart workflow (e.g. after a config change). |
| FUSE mount: `create`/`mknod`/`fsync` not implemented — **all file-creation paths fail with ENOSYS** (editors AND shell `>` redirect) | Surfaced during 2026-05-25 live smoke of the daemon spec. Initially seen via `nano hello.txt` failing with `[ Error writing lock file ./.hello.txt.swp: Function not implemented ]`. Then confirmed broader: `echo X > /tmp/onedrive-smoke/smoke_folder/hello.txt` ALSO fails with `bash: /tmp/onedrive-smoke/smoke_folder/hello.txt: Function not implemented`. The basic FUSE op set landed in earlier work (commits `925eeca` / `1933867` / `9e08915` on `unidrive-mount-linux` — POLISH verdict on 2026-05-24): `read`, `write`, `open` (on EXISTING files), `mkdir`, `unlink`, `rmdir`, `list`, `getattr`. NOT implemented: `create` (open with O_CREAT, used by every "make a new file" path — bash redirect, editor save, `touch`, `cp` destination), `mknod` (legacy fallback), `fsync` (no-op semantics acceptable). Any of those returning `ENOSYS` breaks file creation entirely. Scope: implement `create` (preferred — combines open + creat in a single FUSE op since the 7.23 protocol) in `unidrive-mount-linux/mount/src/fuse_fs.rs`. The new op routes to the JVM `hydration.open_write` verb (exists from Phase 2 namespace-verbs work). `mknod` as legacy fallback to the same handler. `fsync` as a coroutine-safe no-op (the hydration RPC already commits via the JVM's StateDatabase). Acceptance: `echo "x" > /mount/test.txt` succeeds; `cat /mount/test.txt` returns "x"; the file appears on the remote provider; `nano /mount/test.txt` saves successfully (swap-file lifecycle: create → fsync → unlink). Priority **High** — without this, the mount is *unwritable* for new files. Only pre-existing files (re-discovered via refresh) can be opened/edited. Sibling repo, not JVM. |
| Cut first unidrive-mount-linux release tarball + wire dist/install.sh to download + SHA256-verify it | The current install.sh prints a stub message; replace once a release tarball + SHA256 lands on the sibling repo. The download URL pattern + verification step are the actual deliverable. Drop the verified binary at `~/.local/lib/unidrive/unidrive-mount`. Once shipped, `unidrive mount <path>` works end-to-end without a manual `cargo build`. |
| Legacy SyncEngine trips UD-265 deletion-safeguard on the internxt_gernot_krost_posteo profile, daemon stays failed | `unidrive sync --watch` (the systemd-user-unit entry point) against the 195k-file Internxt profile reaches Reconciled state then crashes with `Sync cycle failed (attempt 1), retrying in 10s: UD-265 Deletion safeguard: 150 deletes planned, exceeding max_delete_absolute=50`. The retries don't recover; systemd marks the service `failed`. The 150 deletes likely reflect a real local/cloud divergence accumulated during prior failed runs, but the safeguard is doing its defensive job and won't auto-resolve. Three resolution paths: (a) operator runs `unidrive sync --force-delete` once to clear the divergence, then restarts the service; (b) operator switches the profile to the tracking-set engine (`unidrive ts sync`), which is structurally immune to the phantom-row deletion-cascade class this safeguard protects against; (c) increase `max_delete_absolute` per-profile if the divergence is expected. Surfaced during Phase 2 smoke test attempting to mount the profile via FUSE — the daemon's failure means no IPC socket, so `unidrive mount` cannot connect. `core/app/sync/src/main/kotlin/org/krost/unidrive/sync/SyncEngine.kt` (UD-265 reference). |
| Reach the 5+5+2 smoke target | Current live-integration surface is 4 onedrive (`LiveGraphIntegrationTest`, `OneDriveIntegrationTest`, `OneDriveDeltaIntegrationTest`, `DeltaDiagnosticTest`), 1 internxt (`InternxtIntegrationTest`), 0 sync, plus `CliSmokeTest`. Target: 5 onedrive, 5 internxt, 2 sync (auth/upload/download/delete/delta-reconciles per provider; two end-to-end sync round-trips). Each gated by `UNIDRIVE_INTEGRATION_TESTS=true`. Reducing the existing unit-test surface (≈120 files) is a separate per-test review: don't sweep wholesale, lift the smoke set up to the target first. |
| Tracking-set engine: verify Internxt provider end-to-end | `unidrive ts sync --dry-run` against a real Internxt profile + a live-integration test gated by `UNIDRIVE_INTEGRATION_TESTS=true`. Owns 429-storm / JWT-refresh / malformed-delta interactions in the engine path; surface concrete issues as follow-up entries rather than fix them inline. Blocks closing the Critical-tier tracking-set entry. |
| Tracking-set engine: verify OneDrive provider end-to-end | Same shape against a real OneDrive profile. Owns delta-cursor / 410-Gone-resync / FastBootstrap interactions in the engine path. Blocks closing the Critical-tier tracking-set entry. |
| OneDrive 410 Gone resync handling | Honor `resyncChangesApplyDifferences` / `resyncChangesUploadDifferences` with `Location` restart on the delta loop. |
| OneDrive webhook lifecycle events + validation endpoint | Handle `reauthorizationRequired`, `subscriptionRemoved`, `missed`; add an in-repo HTTP endpoint that echoes `validationToken` within 10 s. |
| OneDrive `file.hashes` in local change detector | Remote-side comparison already uses it (`Reconciler.kt:171` — `remoteItem.hash != entry.remoteHash`); local scanner still mtime+size only (`LocalScanner.kt:102`). Avoids re-uploading on touch-only changes. |
| OneDrive `If-Match` precondition on `createUploadSession` | Catches concurrent edits before the session URL is handed out. |
| OneDrive refresh `downloadUrl` on `assertNotHtml` | When the CDN serves an HTML throttle page, re-resolve via `getItemById` instead of failing the download. |
| XDG-user-dir locale aliasing across `Pictures`/`Bilder`/`Imágenes`/`Images`/… | Local OS standard folder names are locale-aware (`Pictures` en, `Bilder` de, `Imágenes` es, `Images` fr); the cloud holds one canonical name (whichever was created first). Sync sees the locale-renamed local folder as a NEW folder and emits `mkdir-remote` for the local-locale name, producing duplicate parallel trees OR repeated 409-Conflict / `Folder not found` failures during apply. Live evidence: ~3,000 `mkdir-remote /Pictures/...` actions during a real sync where the cloud already had `/Bilder/...`, AND an in-flight `mkdir-remote /Pictures/Scans` that failed with `Folder not found: Pictures in /Pictures` because a `move /19notte78/Pictures -> /Pictures` was queued later. Fix: detect XDG-user-dir aliases at sync time (parse `~/.config/user-dirs.dirs` + a static well-known-alias table); map local-locale → cloud-canonical (whichever exists first OR configurable per-profile override); pin via a smoke test that creates a German `Bilder/` locally against a cloud `Pictures/` and asserts no duplicate is created and no 409 fires. |
| Apply-phase action ordering must respect parent-folder dependencies (mkdir-before-move; child-after-parent) | Reconciler emits actions in a discovery order that doesn't topologically respect path-prefix dependencies. Live evidence: engine emitted `mkdir-remote /Pictures/Scans` BEFORE `move /19notte78/Pictures -> /Pictures` — the mkdir failed with `ProviderException: Folder not found: Pictures in /Pictures` because the parent doesn't exist on the cloud yet (it's still at `/19notte78/Pictures`, scheduled to move later). Similar shape: every child `mkdir-remote /A/B/C` should run AFTER `mkdir-remote /A/B` succeeds. Fix: topological-sort the action list by path-depth + treat `MoveRemote` as both producer (destination) and consumer (source) in the dependency graph; apply in topo-order. Smoke: seed an empty cloud and reconcile a local tree with 3 nesting levels + an internal-move, assert apply succeeds without any "parent not found" or 409 retries. |
| `--fast-bootstrap` doesn't adopt-on-match for local folders that already exist on cloud | `--fast-bootstrap` adopts the remote cursor so REMOTE items stay invisible until they next mutate. But the LOCAL scanner still emits `mkdir-remote` for every local folder, including those whose name matches an existing cloud folder — every such mkdir lands as a 409 Conflict. Live evidence: after `unidrive sync --reset --fast-bootstrap`, the first 14 actions were `mkdir-remote /.safe`, `mkdir-remote /.tresor`, `mkdir-remote /AfA - Gründungsprozess`, `mkdir-remote /Annika Pixel 4`, `mkdir-remote /Pictures`, `mkdir-remote /_INBOX`, `mkdir-remote /dev`, …, ALL of which fired `409 Conflict: A file with the same name already exists in destination folder`. Cost: N 409s per reset, where N = number of top-level local folders that happen to match the cloud. Fix: at fast-bootstrap apply time, adopt-on-name-match for top-level folders (lookup remote by name, if found register the row + skip the mkdir); generalises to the "adopt-on-content-match" pattern from the deferred tracking-set migration entry. Smoke: profile with N top-level matching folders, `--reset --fast-bootstrap`, assert 0 409-Conflicts and N quietly-adopted rows. |
| `status` and `status --all` enumerate different profile sources (orphan-profile-dir divergence) | `unidrive status` discovers profiles by scanning `~/.config/unidrive/*/` (FS state); `unidrive status --all` enumerates only `[providers.*]` sections in `config.toml`. The two diverge when a profile dir exists on disk without a corresponding `config.toml` declaration ("orphan profile") — common after a manual `unidrive auth` against a type-resolved profile name. Live evidence: `status` showed profile `internxt` (sparse=191,671, hydrated=16 GiB, last-sync=never); `status --all` showed only `internxt_gernot_krost_posteo` (the lone `config.toml` declaration), missing `internxt` entirely. Fix: pick a single source of truth (recommendation: FS-scan with `config.toml` as ordering/configuration overlay), apply consistently across both views; flag orphan dirs explicitly (e.g. `[ORPHAN]` glyph in the STATUS column). Rendering parity is a separate property — both views must use identical columns / units / rounding (currently they do, but pin via a snapshot test). Augments the now-closed "`unidrive status --all` enumerates only one profile" entry which fixed config.toml-side enumeration but not the FS-vs-config divergence. |
| Internxt delta-page ancestor-uuid drop is not self-healing on unchanged remote | First-pass enumeration dropped 168 files + 120 folders (288 items) whose ancestor folder UUIDs were absent from the same page's `folderMap`; provider returned `DeltaPage(complete=false)` and the engine correctly suppressed deletes. The WARN ("Dropped items recover when their ancestors next change or via `--reset`") implies a second pass against an unchanged remote keeps producing incomplete plans indefinitely. Fix options: (a) synchronous re-fetch of the missing ancestor on miss; (b) cursor logic re-enqueues affected ancestors on next pass. Evidence: live `TrackingEngineInternxtLiveTest` run against a 195,740-file profile, `core/providers/internxt/src/main/kotlin/org/krost/unidrive/internxt/InternxtProvider.kt:1158-1198`. Surfaced by tracking-set Internxt verification; see `docs/dev/findings/tracking-set-internxt.md`. |
| Tracking-set engine wastes full re-enumeration per pass on OneDrive | `TrackingEngine.enumerateRemote()` correctly acquires and drains OneDrive's `@odata.deltaLink`/`@odata.nextLink` page chain within a single pass, then **discards** the final `deltaLink` at end of pass. Every next `ts sync` re-walks the full remote inventory. Live evidence on `posteo_onedrive`: 3,232 files, 23.55 s per pass — fine here, but scales linearly with remote inventory rather than delta since last pass. For the Internxt-scale case (~196k files) this is the difference between minutes-per-pass and hours-per-pass. Different concern from the existing Medium-tier "persist delta cursor per profile" entry: that one is the data-structure concern (where in `tracking.db`); this one is the OneDrive-specific evidence that the gap bites in practice and needs to land before the tracking-set engine can be the default. `core/app/sync-tracking/src/main/kotlin/org/krost/unidrive/tracking/TrackingEngine.kt:309-353`. Surfaced by tracking-set OneDrive verification; see `docs/dev/findings/tracking-set-onedrive.md`. |
| OneDrive provider has no explicit 410-Gone resync handling on the delta endpoint | `GraphApiService.kt:521-542` handles 410/404 only for upload-session reuse (`uploadLargeFile` two-attempt loop); the `getDelta` code path at `:163-180` has no Gone handling. When OneDrive returns 410 Gone on a stored cursor (cursor too old, drive re-keyed), the engine receives a `ProviderException`, `TrackingEngine.enumerateRemote` catches and sets `complete=false`, the lemma + suppression backup correctly protects the user from spurious deletes — but the engine then loops on enumeration-incomplete plans indefinitely until something else triggers a fresh enumeration. Fix: auto-resync from latest on `410 Gone` in `OneDriveProvider.delta()` — the `deltaFromLatest()` capability already exists at `OneDriveProvider.kt:205-215`. Different from the existing High-tier "OneDrive 410 Gone resync handling" entry which covers `resyncChangesApplyDifferences` / `resyncChangesUploadDifferences` for the legacy `SyncEngine` delta loop. `core/providers/onedrive/src/main/kotlin/org/krost/unidrive/onedrive/OneDriveProvider.kt:153-176`. Surfaced by tracking-set OneDrive verification; see `docs/dev/findings/tracking-set-onedrive.md`. |
| OneDrive delta loses sight of a path the engine itself just wrote ("not in delta, marking deleted" loop) | Live evidence (2026-05-24 against `posteo_onedrive`, log entries `08:25:12`, `08:25:23`): the `--watch` loop logs `Full sync: DB entry /geheim.txt (remoteId=8AB037CBAB5FB739!s…) not in delta, marking deleted` for a path that the same engine successfully uploaded seconds earlier (multiple `Upload (simple): /geheim.txt (52 bytes)` entries in the surrounding minutes — 200 OK each). The full-sync codepath enumerates OneDrive's delta and concludes the path is gone, then queues a deletion; on the next cycle the local file is detected again, the engine re-uploads, then the next full-sync pass declares it deleted again. Cycle is benign because the deletion is path-recreated by the next upload, but it produces a steady-state churn that re-uploads the same file every minute and pollutes the delta stream. **Hypotheses to investigate, ordered by likelihood:** (a) the engine reads the delta page before OneDrive has indexed the just-completed PUT — Graph's delta endpoint has eventual-consistency vs. the item-write endpoint, so a tight upload-then-delta sequence can miss the new item; (b) the engine's delta cursor is paginating mid-stream and the file lives on a later page that the full-sync codepath doesn't fully drain before declaring "not in delta"; (c) the local sync root's `~/Onedrive/geheim.txt` keeps drifting against `state.db.localMtime` (mtime racing the upload completion), retriggering the upload even though server-side bytes already match. First step: capture a single instance of the cycle with TRACE-level Graph logging enabled so the delta-page contents are visible; cross-reference with the upload's `req=…` request-id timing. `core/app/sync/src/main/kotlin/org/krost/unidrive/sync/SyncEngine.kt` (the `not in delta, marking deleted` log site is the lookup table to find the exact line). |

## Medium — efficiency

| Title | Scope |
|---|---|
| OneDrive `$batch` for reconciliation reads | Max 20 per batch; `dependsOn` ordering; per-inner 429 retry. Biggest single throughput win on bulk-share / bulk-move. |
| OneDrive `Range` against `@microsoft.graph.downloadUrl` | Parallel-segment download. |
| OneDrive conditional GET via `If-None-Match` + cTag | Make unchanged-file refreshes free. |
| OneDrive `$select` slim payload on delta | Minimum: `id,cTag,size,file/hashes,@microsoft.graph.downloadUrl`. |
| OneDrive `conflictBehavior` user-selectable | Hard-coded `"replace"` at `GraphApiService.kt:352` (simple-PUT) and `GraphApiService.kt:649` (createUploadSession); once parity is fixed in the data-risk item. |
| OneDrive `createLink` client-side dedupe | Check `listPermissions` for an existing matching link before POSTing. |
| OneDrive per-tenant concurrency calibration | Personal vs Business vs GCC; flagged in `core/providers/onedrive/README.md`. |
| OneDrive `HttpRetryBudget` per-provider override | Constants currently global. |
| Tracking-set engine: persist delta cursor per profile | Engine currently re-enumerates the full remote every pass. Persist cursor in `tracking.db` so subsequent passes see only changes. Required before tracking-set becomes the default engine. |
| Tracking-set live-test runtime is impractical for routine CI | `TrackingEngineInternxtLiveTest` took 62 min 34 s for one `syncOnce()` against a 195k-file Internxt account — unsuitable for any pre-commit or per-PR loop. Options: gate behind a separate Gradle task (e.g. `:app:sync-tracking:liveTest`); add a test-side `maxItems` hook so the smoke variant runs in <5 min while a nightly variant runs full; or scope the live test to a synthetic small profile and keep the real-account run as a manual command. `core/app/sync-tracking/src/test/kotlin/org/krost/unidrive/tracking/TrackingEngineInternxtLiveTest.kt`. |
| Tracking-set engine: migration / coexistence with legacy `state.db` | First `ts sync` on a profile with an existing `state.db` runs adopt-on-content-match against live observations rather than importing legacy rows. Document bulk-collision rendering for fresh-adopt operators; consider a `--auto-match` switch (size or name) for providers without a content hash. |
| Socket-path name hashing diverges from hydration cache root naming for long profile names | `IpcServer.socketBaseName(profileName)` (`core/app/sync/src/main/kotlin/org/krost/unidrive/sync/IpcServer.kt:614-620`) hashes the profile name to a 4-byte SHA-1 hex digest when the full socket path would exceed `MAX_SOCKET_PATH_LENGTH` (~108 bytes on Linux), producing `/run/user/<uid>/unidrive-<hash>.sock` plus a `.meta` sidecar that maps the hash back to the original name. `SyncEngine.hydrationCacheRoot(...)` (`core/app/sync/src/main/kotlin/org/krost/unidrive/sync/SyncEngine.kt:2966-2969`) uses `providerId.ifBlank { "default" }` verbatim as a directory name — no hashing, no truncation, no escaping. For a profile name long enough to trigger socket hashing, the IPC socket lives at the hashed path while the hydration cache lives at the verbatim-name path. The `.meta` sidecar recovery means IPC still works, but the two paths now disagree about what "identifies this profile on disk." Today this is benign (mount and sync don't reconcile against each other's cache view), but it's a latent landmine: any future cross-process cache-coordination logic (cache eviction triggered by daemon refresh; UI sidebar showing both socket and cache paths; debug tooling) that derives one path from the other will silently disagree. Two-sources-of-truth audit 2026-05-24. Scope: pick one canonical sanitised-name function (e.g. `Profile.canonicalDiskName(name): String` that does the SHA-1 truncation IF the unhashed name would break ANY path consumer), use it consistently for socket name + cache root + future per-profile directory naming. Acceptance: a test exercises a profile name long enough to trigger socket hashing and asserts the cache root uses the same canonical name. Priority Low — defensive hardening, no live bug today. |
| Pending-upload predicate (`remoteId == null && isHydrated == true`) is enforced informally across many call sites | UD-901 defines a pending upload as `remoteId IS NULL AND isHydrated == TRUE`. Current call sites check the predicate split or partial: `StatusCommand.kt:148` counts upload-pending bytes by `remoteId == null` alone; `Reconciler.kt` recovery loops check `isHydrated == true` without always also asserting `remoteId == null`; `LocalScanner.kt:84-98` writes rows with `remoteId = null` where `isHydrated` is derived from the `sparseLeftover` heuristic. A row with `remoteId = null && isHydrated = false` (sparse/partial-download row that was then locally modified) sits in the gap: the full UD-901 predicate excludes it (so the status counter underflows on bytes-to-upload) but downstream apply-phase code might still try to upload it because half the predicate matches. Two-sources-of-truth audit 2026-05-24: the predicate is the source of truth but the enforcement is "every consumer remembers to check both halves." Scope: (a) introduce a typed predicate (e.g. `SyncEntry.isPendingUpload: Boolean` field computed in the data class) so consumers can't accidentally check half; (b) add a pinned test that constructs a row with `remoteId = null && isHydrated = false` and asserts both halves of the predicate matter; (c) audit all call sites that grep for `remoteId == null` or `isHydrated` and either rewrite to the typed predicate or document why the split-check is intentional. Priority Low — no live bug today, but the kind of correctness contract that silently regresses on the wrong refactor (CLAUDE.md "orthogonal invariant decomposition" rule). |
| Pre-reconcile empty-sync_root guard must differentiate "rehydrate intent" from "wrong sync_root" | `SyncEngine.kt:686-700` fires when state.db has hydrated rows but `sync_root` is empty, and recommends `--force-delete`. That recommendation is the bidirectional override and semantically wrong for a `--download-only` user who wiped local intentionally and wants the cloud copy back. Options: (a) new `--rehydrate-from-cloud` flag that asserts intent and skips the guard; (b) detect `--download-only` in the guard and auto-skip (destructive direction is already gated); (c) make the message conditional on the direction filter. Without this, the documented escape hatch points at the wrong action class. Companion cleanup: `SyncCommand.kt:428` calls `Files.createDirectories(config.syncRoot)` BEFORE the guard runs, so even an aborted run leaves an empty sync_root behind — move the create AFTER the guard. |
| Typed `FolderNotEmpty` provider exception for `HydrationImpl.rmdir` (namespace-verbs R2) | Follow-up filed at close-out per `docs/dev/specs/hydration-namespace-verbs-design.md` §5 R2. `HydrationImpl.rmdir` currently detects non-empty folders by substring-matching `"not empty"` / `"non-empty"` against the provider exception's `message` (per spec §3.4). The spec calls this "intentionally string-matchy because the provider SPI doesn't expose a typed `FolderNotEmpty` exception today" — fragile against provider error-wording changes (e.g. localised Microsoft Graph error text). T3 (`rmdir_detects_provider_not_empty_substring`) pins both OneDrive and Internxt current wordings, so a wording change fails loudly at test time rather than silently regressing the ENOTEMPTY surface to EIO. Scope: introduce a typed `FolderNotEmpty` exception (or sealed-class variant) at the provider tier (`CloudProvider` SPI), update OneDrive + Internxt implementations to throw it on the 409 / non-empty surface, replace the substring match in `HydrationImpl.rmdir` with `catch (e: FolderNotEmpty)`. Acceptance: T3 rewritten to assert the typed exception is caught (not the substring); the two known wordings no longer appear in `HydrationImpl.kt`. Priority Medium — hardening, not a live bug. |
| `mkdir` parent-missing maps to ENOENT instead of EIO (namespace-verbs R3) | Follow-up filed at close-out per `docs/dev/specs/hydration-namespace-verbs-design.md` §5 R3 and risk R3. Today `HydrationImpl.mkdir` against a non-existent parent throws a generic provider exception (OneDrive 404 / Internxt similar) which the IPC handler surfaces as `{"ok":false,"error":"<raw provider message>"}` → co-daemon `map_ipc_err_to_errno` falls through to the `_ => EIO` arm. POSIX `mkdir` returns `ENOENT` in this case. The spec defers correct mapping because "the cost of mapping it correctly is substantial (parse the provider error to detect 'parent missing'), and it's a corner case (`mkdir -p` from userspace creates intermediate folders one at a time, each is its own FUSE call against a known parent)." Scope: add `MkdirResult.ParentNotFound` sealed-class variant; detect via typed provider exception (couples with R2-style provider-tier work) or substring match as an interim; emit wire-contract error `"parent_not_found"`; extend co-daemon `map_ipc_err_to_errno` with a matching arm mapping to `libc::ENOENT`. Acceptance: a new JVM test pins the `MkdirResult.ParentNotFound` path, a new Rust integration test asserts `Errno::ENOENT` reaches the kernel. Priority Medium — works for the common `mkdir -p` case but silently EIO's the parent-missing edge. |
| Cache-file eviction on `unlink` / `rmdir` (namespace-verbs R5) | Follow-up filed at close-out per `docs/dev/specs/hydration-namespace-verbs-design.md` §5 R5 (and risk R5 / NG4). When `HydrationImpl.unlink` or `rmdir` succeeds against the provider and the StateDatabase row is marked deleted, the local hydration cache file under `~/.cache/unidrive/hydration/<provider>/<path>` is NOT evicted. The spec records this as "disk-space concern, not correctness" — but a subsequent re-create at the same path could hit a stale cache entry, and the leaked bytes accumulate until the existing cache-eviction logic reaps them later. Scope: after a successful provider delete + StateDatabase update, `Files.deleteIfExists(cacheRoot.resolve(path))` for unlink; recursive `Files.walkFileTree`-based deletion for rmdir. Must be idempotent — partial cleanup must not abort the whole op (the cloud delete already succeeded). Acceptance: a JVM test creates a fake cache file, exercises unlink, asserts the file no longer exists; symmetric test for rmdir with a populated subtree. Priority Medium — edge case until users start aggressively recycling paths. |
| Convert `unidrive sync` into a client of the `unidrive daemon` process (daemon-decomposition phase 2) | Follow-up to the High-tier mount-IPC-lifecycle entry. Once the `unidrive daemon` process ships per `docs/dev/specs/unidrive-daemon-design.md` (in progress) as the minimal-scope unblocker for mount, `unidrive sync` still owns its own IpcServer + reconcile loop in-process (transitional state). This entry tracks the follow-on work to make `sync` a thin RPC client of the daemon: the daemon hosts the reconcile loop as a scheduled task or RPC-triggered job; the `sync` CLI streams progress events back to the operator's terminal via the existing IpcServer's `sync.subscribe` subscriber-set mechanism. After this lands, the mode-mutex code (`ProcessLock.Mode` + `acquireProfileLockForMount` factory + zombie-mount detector) becomes deletable scaffolding — the daemon serialises everything internally per-profile. Scope: move reconcile-loop ownership from `SyncCommand` to a daemon-side scheduler; introduce `sync.run` / `sync.cancel` RPC verbs; rewrite `SyncCommand` as a client that issues `sync.run` then subscribes to progress events; preserve all existing CLI flags (`--dry-run`, `--watch`, `--full-tree`, etc.) as RPC parameters. Acceptance: `unidrive sync` exit semantics and stdout output are byte-for-byte equivalent to today; all existing sync integration tests pass against the new client path; the mode-mutex code is removed in the same commit (or a one-line follow-up). Priority Medium — meaningful refactor, no behaviour change for the operator. Brainstorm 2026-05-24 decomposition path (a). |
| Auto-spawn `unidrive daemon` on first client connection (daemon-decomposition phase 3) | Follow-up to the High-tier mount-IPC-lifecycle entry. The minimal-scope daemon spec (`docs/dev/specs/unidrive-daemon-design.md`) requires the operator to start the daemon explicitly via `unidrive daemon start <profile>` before `unidrive mount` will work. This is the UX equivalent of asking the user to `systemctl --user start unidrive` — discoverable but extra friction. This entry tracks the follow-on work to make daemon spawn automatic: when a client (`mount`, `sync`-as-client per phase-2 entry, `unidrive-ui`) attempts to connect to `/run/user/<uid>/unidrive-<profile>.sock` and gets `ECONNREFUSED`, the client first attempts to spawn the daemon (`fork+exec` or systemd-user-unit start) and retries the connection with a bounded backoff. Lifecycle complications: who owns the daemon's stderr/stdout? auto-spawn from `mount` makes the daemon a child of the mount process — mount exits, daemon exits — that's wrong; the daemon must outlive any single client. Approaches: (a) daemon `fork` + `setsid` + redirect to `~/.local/state/unidrive/daemon-<profile>.log`; (b) generated systemd-user-unit per profile, `systemctl --user start unidrive-<profile>.service`. Acceptance: fresh-machine workflow `unidrive auth posteo_onedrive && unidrive mount /tmp/x` succeeds without the user having seen the word "daemon"; daemon process survives across multiple client connect/disconnect cycles; `unidrive daemon status` correctly identifies an auto-spawned daemon. Priority Medium — UX polish, daemon is functional without it. Brainstorm 2026-05-24 decomposition path (c). |
| `unidrive daemon status` should refuse / warn when lock-holder mode is not `daemon` (currently prints misleading "daemon status" for a sync process) | Surfaced during 2026-05-25 live smoke. With a stale `.lock.pid` left by a legacy `unidrive sync` watcher (pre-Phase-1-mode-mutex), `unidrive daemon status` prints `pid <N>, mode (no-mode)` followed by trying to RPC the daemon socket (which doesn't exist because the lock-holder is a sync process, not a daemon). The user sees a misleading two-line output that LOOKS like daemon status for an entirely different process class. Symmetric to `daemon stop` which already refuses with a clear error when `modeToken != "daemon"`. Fix: in `DaemonStatusCommand.run()` (`core/app/cli/src/main/kotlin/org/krost/unidrive/cli/DaemonCommand.kt`), after `readLockPid` returns `Present`, check `contents.modeToken == "daemon"`. If not, print to stderr `"lock for profile '<X>' is held by mode '<token>', not 'daemon' (use 'kill <pid>' or the appropriate stop mechanism)"` and exit 1. The legacy pid-only case (`modeToken == null`, rendered as `(no-mode)` today) should be treated the same way (it's pre-Phase-1 sync). Acceptance: `unidrive daemon status` against a stale-sync-or-no-mode `.lock.pid` exits 1 with the mode-mismatch error; against a real daemon it prints the file-derived line + RPC-derived line as today. Priority Medium — misleading UX, surfaces on every machine that ran sync before the daemon spec landed. |
| Daemon doesn't auto-poll for remote changes — mount view stays stale until operator runs `refresh` | Surfaced during 2026-05-25 live smoke. Operator renamed a file on the OneDrive webapp; 3 minutes later, `ls /tmp/onedrive-smoke` still showed the old name. Behaviour is correct per spec G3 ("strictly reactive — only acts when an IPC verb arrives") + NG5 ("no persistent refresh cursor; new refresh.run re-enumerates from scratch") of `docs/dev/specs/unidrive-daemon-design.md`. Workaround: `unidrive -p <profile> refresh`. But the UX surprise is real: operators expect FUSE mounts to reflect remote state within seconds, not "next time you explicitly refresh." Two resolution paths: (a) add a `--poll-interval <duration>` flag to `unidrive daemon run` that fires `refresh.run` periodically in-process (e.g. every 60s default; off when 0); (b) wait for the phase-2 follow-up ("Convert `unidrive sync` into a client of the `unidrive daemon` process") which moves the reconcile loop into the daemon and inherently provides continuous remote-change visibility. Path (a) is cheap; path (b) is the long-term answer. Acceptance for (a): `unidrive -p <profile> daemon run --poll-interval 60s` triggers `refresh.run` every 60s automatically; mount-side `ls` reflects remote renames within one poll interval. Spec amendment required (`G3` strictly-reactive contract gets a `--poll-interval` exception). Priority Medium — design-intentional today, UX-surprising in practice. |
| `RefreshRpcHandler` in-flight guard ordering + missing error-path test coverage | Task 8 of `docs/dev/plans/unidrive-daemon-plan.md` introduced `RefreshRpcHandler` with an `AtomicReference<RefreshJob?>` in-flight guard. The `scope.launch { ... ; inFlight.set(null) }` runs AFTER `inFlight.set(RefreshJob(...))` — under any normal dispatcher (BlockingEventLoop, IO threadpool, etc.) the launched body cannot start before `handle()` returns, so this is safe in practice. With `Dispatchers.Unconfined` (or a pathological synchronous interceptor) the launched body could execute its `finally { inFlight.set(null) }` before `set(RefreshJob)` runs, leaving the guard stuck non-null. Not currently exploitable (serveScope carries a real dispatcher), but defensive style would call `inFlight.compareAndSet(null, RefreshJob(jobId, ...))` BEFORE launch with cleanup on launch-failure. Additionally, three of the four documented behaviours of `RefreshRpcHandler` are unverified by tests: the `busy` reply path (needs a long-running SyncEngine stub), the `provider_error` path (needs a throwing SyncEngine stub), and the `shutdown` (CancellationException) path (needs triggering daemon close during in-flight refresh). T5 covers only the happy path + non-busy re-issue. Per CLAUDE.md "orthogonal invariant decomposition" rule each error category should have a named test. Scope: (a) defensive `compareAndSet` refactor of the in-flight guard; (b) three new named tests `refresh_run_returns_busy_when_concurrent`, `refresh_run_emits_provider_error_on_engine_failure`, `refresh_run_emits_shutdown_on_daemon_close`. Priority Medium — defensive hardening + test coverage for documented behaviour. |

## Low — guards and UX

| Title | Scope |
|---|---|
| `unidrive-ui` needs to call `sync.subscribe` after connecting | After the sync-progress subscriber-set fix landed (commit 0424ba5 + b763f78), legacy clients that connect-and-listen without subscribing receive no events. The systray UI binary at `~/.local/lib/unidrive-ui.jar` (Apr 2 09:58 build) is the suspected one — it'll show stale state until the next deploy. Fix is a one-line `sync.subscribe` round-trip after the connect in the UI's IPC client. Detection: when launched against a daemon running the new build, the systray shows "no recent sync activity" even mid-sync. |
| OneDrive `malware` facet skip on download | One-line guard. |
| OneDrive `fileSize` precheck → early 507 | Avoid wasted upload attempts. |
| Internxt keep-overwritten prune | `unidrive prune --backups [--older-than 30d] [--profile NAME]` for `keep_overwritten` archives. Glob on `.unidrive-prev-*` in plainName. |
| `unidrive status` no-default-profile fallback shows misleading "Local Filesystem" row | When invoked without `--profile` and with no default configured in `config.toml`, `unidrive status` renders a phantom row labeled "Local Filesystem" with a `⚠︎` marker, `[– –]` status code, and all-zero columns — looks like a real but broken entry, isn't. `unidrive status --all` correctly shows the configured providers. Either resolve a default profile up front and show its row, or emit a one-line message ("No default profile configured. Use --all to list, or --profile <name> to select.") and exit zero. The current placeholder is worse than either alternative because it suggests something is broken. |
| `unidrive status` shows `[✔ OK]` on stale cached auth state | When the persisted state row is from a previous sync where the token was valid, but the current token has since expired (and `status` hasn't probed the provider yet), the STATUS column still renders `[✔ OK]` — same as a freshly-verified profile. Should render a stale-state glyph (e.g., `⚠︎ STALE`, `⏳`) when the displayed data predates the current auth-cache invalidation. Distinguish "looks healthy because we just checked" from "looks healthy because we haven't re-checked since the token went stale." |
| Build-time test that `logback.xml` is XML-well-formed | One-line JUnit test under `core/app/cli/src/test/` running `DocumentBuilder.parse()` against the bundled `logback.xml`. Closes the "CI green, logging silently broken" failure mode where logback's parse-error fallback silently substitutes defaults — caught self-inflicted during the MDC empty-slot fix, where a `--` literal inside an XML comment (forbidden per XML 1.0 §2.5) failed to trip `./gradlew check`. |
| Build-time test that `BuildInfo.DIRTY` correctly handles mode-only diffs | Run `git diff HEAD --numstat` against a synthetic mode-only diff and assert `dirty=false`; inverse assertion against a content change pins `dirty=true`. Pins the invariant the just-landed mode-only-ignore fix relies on; today verified only by manual inspection. |
| Hydrated-but-locally-missing rows in `--download-only` mode should re-download, not silently loop | When local file is missing but `state.db` has `is_hydrated=1`, the Reconciler emits `DeleteRemote` — the `(DELETED, UNCHANGED)` recovery downgrade at `Reconciler.kt:655` only catches `!isHydrated`. `--download-only` then filters the action; benign and infinite — every future sync replans + filters the same N actions, state.db never converges. Live evidence: a `--force-delete --download-only` real sync against a wiped sync_root produced 31 stranded rows that will replay every cycle until manually reconciled. Fix: extend the recovery downgrade to also fire when `entry.isHydrated && !localPresent`, gated by the same `--force-delete` policy. Smoke: seed one hydrated DB row with no local file, walk empty syncRoot, assert exactly one `DownloadContent` (not `DeleteRemote`). |
| Tracking-set live test: JWT/OAuth-refresh path not structurally exercised | A 62-minute enumeration against a real Internxt profile produced zero token-refresh log lines — either the JWT TTL exceeded the run length, or refreshes fired silently with no externally observable signal. The 23.55 s OneDrive pass on `posteo_onedrive` likewise didn't span a token refresh (OneDrive token TTL ~60 min, run started minutes after a fresh `unidrive auth`). The tests pass but don't prove the refresh path works on either provider. Add either short-TTL token variants for stress runs, or refresh-counter hooks the tests can assert against, so refresh-during-enumeration is structurally covered on both providers. Surfaced by tracking-set Internxt + OneDrive verifications; see `docs/dev/findings/tracking-set-{internxt,onedrive}.md`. |
| Tracking-set live test: throttling / 429-storm handling path not exercised | A single bulk enumeration's natural request rate didn't trip Internxt's rate limiter on a 195k-file account, and the OneDrive 3,232-file pass didn't trip Microsoft Graph's per-app rate budget either. The engine's throttling-handling interaction remains unverified end-to-end on both providers. Add stress variants with parallel `delta()` callers, or fault-injection seams that synthesise 429 / `Retry-After` responses, so the retry-budget code paths are structurally covered on both providers. Surfaced by tracking-set Internxt + OneDrive verifications; see `docs/dev/findings/tracking-set-{internxt,onedrive}.md`. |
| Gradle daemon poison: `java.io.EOFException` from `SerializableTestResultStore` with 0-byte results = stop the daemon | During 2026-05-25 implementation of `docs/dev/specs/unidrive-daemon-design.md` spec, a Gradle daemon went into a wedged state after a custom task registration was added to `core/app/cli/build.gradle.kts` during scratch debugging. Symptoms: every `:app:cli:test` invocation immediately failed with `java.io.EOFException` raised by `org.gradle.api.internal.tasks.testing.results.serializable.SerializableTestResultStore.openAndInitializeDecoder` reading a 0-byte `results-generic.bin`. The test worker JVM never spawned. Diagnostics that pointed to the cause: (a) running the same code via a scratch `main()` on the gradle-managed JDK 21 succeeded in ~1s with correct output; (b) unrelated test classes (e.g. `AnsiHelperTest`) ALSO failed with the same EOFException; (c) `./gradlew --stop` immediately fixed both. Cost: ~25 minutes of false-trail debugging before trying `--stop`. Recommended fix: file this playbook in `docs/dev/lessons/gradle-daemon-poison.md` (new file) so future contributors recognise the symptom and try `./gradlew --stop` first. Priority Low — operational hygiene, not a code bug. |
| `RefreshRpcHandler` exception-message JSON escape covers quotes only | `RefreshRpcHandler.kt` line ~62 escapes only double-quotes in exception messages before embedding them in the `refresh.done` terminal event: `e.message?.replace("\"", "\\\"")`. JSON strings also require escaping `\\`, control characters (`\n`, `\r`, `\t`, `\b`, `\f`), and bytes below `0x20`. Kotlin exception messages with backslashes (e.g. a Windows path in a stack trace) or newlines (multi-line error messages) would produce invalid JSON. Scope: replace the inline `.replace(...)` with a proper JSON encoder. The codebase already uses one — verify via `grep -rn 'org.json\|kotlinx.serialization' core/app/cli/src/main/`. Acceptance: a test that constructs an exception with `message = "path with \\ and \"quotes\" and \nnewlines"` round-trips through the `refresh.done` event without producing invalid JSON. Priority Low — fires only on exotic exception messages, never observed in production. |
| `.lock.pid` rendered as `mode (no-mode)` for legacy pid-only sidecars — confusing UX | Companion to the Medium-tier daemon-status-mode-mismatch entry. Today `DaemonStatusCommand` falls through to the literal string `"(no-mode)"` when `parts.getOrNull(1) == null` (legacy pid-only format from pre-mode-mutex sync watchers). The implementation is correct per spec §3.1 forward-compat, but the operator-visible string is ambiguous: does "(no-mode)" mean "the sidecar has no mode token" or "the daemon is in a no-mode state"? Probably folds into the mode-mismatch refusal entry — if status exits 1 with `"held by mode 'legacy' or '(no-mode)'"`, the rendering is consistent. If we keep the print-then-RPC two-stage shape, rename `(no-mode)` to something clearer like `legacy (pre-mode-mutex)`. Surfaced during 2026-05-25 live smoke. Priority Low — cosmetic, fired only on machines that ran sync before the daemon-mutex landed. |
| `unidrive refresh` against a profile with a pre-existing delta cursor returns only incremental delta, not a full enumeration — operator UX surprise | Surfaced during 2026-05-25 live smoke against `posteo_onedrive` (3232 files). After F7 fix, first `refresh.run` reported `Delta: 1 items, hasMore=false` because OneDrive's delta endpoint resumed from the persisted cursor and returned only the one item that changed since the previous sync (the `WTF.kdbx` rename done via webapp). Result: `ls /tmp/onedrive-smoke` showed exactly one file — technically correct (state.db only knows what delta has revealed), but operator's mental model of "refresh = re-enumerate the whole cloud tree" is violated. The current behavior is correct per spec §4.2 ("delegates to existing SyncEngine refresh path"); the surprise is operator-facing only. Resolution paths: (a) document the delta-cursor reuse behaviour in `unidrive refresh --help` text (one sentence: "Resumes from the last sync cursor; use --reset to re-enumerate from scratch"); (b) folds into the High-tier `--reset` entry — once `--reset` exists, the surprise is self-explanatory (operator who wants full enum reads the help text + uses --reset). Priority Low — UX docs gap, no functional bug. |

## Cross-cutting

| Title | Scope |
|---|---|
| Path normalization (NFC) across sync | Required for cross-platform file matching. |
| Retry budget per operation | Cap retries to keep the sync loop responsive. |
| Provider SPI hardening with two providers | Either trim what isn't earning its keep or extend to remove provider-name dispatch sites. |

## Design constraints (not tickets — bind when related work lands)

- **Platform-surface code lives outside `core/app/`.** Anchor: `AGENTS.md` *What not to do* — "Don't grow the daemon to host a UI tier." Trigger: any work on the Windows desktop, Android, or Linux UI surfaces declared in `docs/adr/multi-platform.md`. Those surfaces consume the core; they don't extend it inline.

## Deferred — post Linux-daemon ship, sequenced by [docs/adr/multi-platform.md](docs/adr/multi-platform.md)

- **Virtual filesystem layer (placeholders for cloud-only items).** Today only physically-downloaded files appear in the sync_root; large cloud accounts (live example: 625 GiB / 171k files) see an empty folder. The two platform-tier implementations that close this gap — Windows CloudFiles API for the Windows desktop client, FUSE for the Linux UI — are the explicit scope of those surfaces in `multi-platform.md`. Daemon-side interim mitigation: a `unidrive get --recursive <path>` (or `unidrive pin <path>` if pin/unpin lands) to bulk-hydrate. The platform implementations are scheduled work, not indefinitely deferred.

- **Tombstone retention policy on `state.db`.** v1 keeps TRASHED rows indefinitely. Includes the permanent-delete drift case — Internxt's ~30-day purge means a TRASHED row may eventually stop appearing in cloud listings; v1 leaves the row as TRASHED rather than promoting to DELETED. Revisit alongside a `tombstone_retention_days` knob if `state.db` growth becomes user-visible.
- **Local-modified-while-TRASHED conflict-aware merge.** Editing the local copy of a file currently in cloud trash, then restoring the cloud copy → conflict. v1 is last-write-wins (cloud restore overwrites the local edit on re-download). Conflict-aware merge would mirror the existing `ConflictPolicy` machinery for the editing-during-trash window.
- **Internxt multipart upload** — constants exist in `InternxtConfig.kt` but unconsumed. Single-shard upload works up to the per-account size cap. Defer because no smoke test currently exercises >shard-size files; revisit when one does.
- **Internxt Shares API** — server has ~30 endpoints; provider returns Unsupported. Not in smoke surface, not required for two-way sync.
- **OneDrive resumable-upload 416/417 nuance** — current chunked retry covers the common case; the 416 GET-probe-and-trim path is the rare edge. Defer until smoke surfaces it.
- **Xtra E2EE re-evaluation** — the `xtra` wrapper was removed in the slim prune. Internxt is natively E2EE; OneDrive users who want client-side encryption no longer have a layer for it. Re-evaluate post-MVP if user demand surfaces.

## Out of scope across all surfaces

- MCP server (removed; not coming back).
- Backup-tool features (snapshots, restore points).
- Three-way merge of document conflicts — surface conflicts, offer keep-both or last-writer-wins.
- Embedding the core as a generic third-party library. First-party platform tiers (Windows desktop, Android, Linux UI) consume the core; there is no generic embedding offer for arbitrary third parties.
- Provider-specific niche features without cross-provider generalization.
