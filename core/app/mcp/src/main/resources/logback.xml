<configuration>
  <!-- Platform-aware log directory: LOCALAPPDATA on Windows, ~/.local/share on Linux -->
  <property name="LOG_DIR" value="${LOCALAPPDATA:-${user.home}/.local/share}/unidrive"/>

  <!-- UD-212: %X{profile:-*} renders the MDC `profile` key.
       UD-234: %X{build:-???????} renders the short git sha from BuildInfo.COMMIT. -->
  <appender name="STDERR" class="ch.qos.logback.core.ConsoleAppender">
    <target>System.err</target>
    <encoder>
      <!-- UTF-8 forced; see core/app/cli logback.xml for rationale. -->
      <charset>UTF-8</charset>
      <pattern>%d{HH:mm:ss.SSS} %-5level [%X{build:-???????}] [%X{profile:-*}] [%X{scan:--------}] %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_DIR}/unidrive-mcp.log</file>
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>INFO</level>
    </filter>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${LOG_DIR}/unidrive-mcp.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
      <maxFileSize>10MB</maxFileSize>
      <maxHistory>5</maxHistory>
      <totalSizeCap>50MB</totalSizeCap>
    </rollingPolicy>
    <encoder>
      <!-- UTF-8 forced; see core/app/cli logback.xml for rationale. -->
      <charset>UTF-8</charset>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%X{build:-???????}] [%X{profile:-*}] [%X{scan:--------}] [%thread] %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <root level="WARN">
    <appender-ref ref="STDERR" />
    <appender-ref ref="FILE" />
  </root>
</configuration>
